import { GameContext } from '../core/GameContext';
import System from '../core/System';

export default class SysInit extends System {
  update(_context: GameContext, _time: number, _delta: number): void {
    // Placeholder system
  }
}
